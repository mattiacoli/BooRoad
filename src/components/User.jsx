{
  /*
  import React from 'react';

export default function ParticipantItem({ user }) {
    return (
        <div className="accordion" id={`accordion-${user.id}`}>
            <div className="accordion-item">
                <h2 className="accordion-header" id={`heading-${user.id}`}>
                    <button
                        className="accordion-button collapsed"
                        type="button"
                        data-bs-toggle="collapse"
                        data-bs-target={`#collapse-${user.id}`}
                        aria-expanded="false"
                        aria-controls={`collapse-${user.id}`}
                    >
                        {user.nome} {user.cognome}
                    </button>
                </h2>
                <div
                    id={`collapse-${user.id}`}
                    className="accordion-collapse collapse"
                    aria-labelledby={`heading-${user.id}`}
                    data-bs-parent={`#accordion-${user.id}`}
                >
                    <div className="accordion-body">
                        <p><strong>Email:</strong> {user.email}</p>
                        <p><strong>Telefono:</strong> {user.telefono}</p>
                        <p><strong>Codice Fiscale:</strong> {user.codiceFiscale}</p>
                        <p><strong>Contatto di Emergenza:</strong></p>
                        <ul>
                            <li><strong>Nome:</strong> {user.contattoEmergenza.nome}</li>
                            <li><strong>Telefono:</strong> {user.contattoEmergenza.telefono}</li>
                            <li><strong>Relazione:</strong> {user.contattoEmergenza.relazione}</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    );
}
*/
}
